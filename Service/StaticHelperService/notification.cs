namespace Service.StaticHelperService
{
    public static class notification
    {


        //public static async Task SaveNotification(ApplicationDbContext context, HttpContext httpContext)
        //{
        //    var nouveauInscris = await context.ClientsPublic.Select(c => new Domain.Model.Notification
        //    {
        //        Nom = c.Nom,
        //        Prenom = c.Prenom,
        //        Statut = c.Statut,
        //        Date = (DateTime)c.DateAderation
        //    }).Take(10).ToListAsync();

        //    SessionHelperRepository.SetObjectAsJson(httpContext.Session, "notification", nouveauInscris);

        //}

    }
}
